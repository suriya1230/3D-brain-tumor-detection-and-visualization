# CPU wheels. For GPU, install torch from https://pytorch.org first, then
# `pip install -r requirements.txt` and let pip keep the existing torch.
torch==2.4.1
monai==1.4.0
nibabel==5.3.2
numpy==1.26.4
scipy==1.14.1
scikit-image==0.24.0
trimesh==4.5.3
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.19
